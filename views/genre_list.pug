extends layout 

block content 
  h1(style='margin-top:20px')= title 

  if genre_list.length 
    ul 
      each genre in genre_list 
        li
          a(href=genre.url) #{genre.name}
  else 
    p1 Список жанров пуст
